import {
  Zap,
  Sparkles,
  Droplets,
  FileText,
  type LucideIcon,
} from 'lucide-react';

export const Icons = {
  Zap,
  Sparkles,
  Droplets,
  FileText,
};

export type IconName = keyof typeof Icons;

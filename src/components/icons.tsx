import {
  Zap,
  Sparkles,
  Wifi,
  Droplets,
  FileText,
  type LucideIcon,
} from 'lucide-react';

export const Icons = {
  Zap,
  Sparkles,
  Wifi,
  Droplets,
  FileText,
};

export type IconName = keyof typeof Icons;

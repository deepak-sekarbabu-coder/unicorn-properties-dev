import {
  Zap,
  Sparkles,
  Droplets,
  FileText,
  type LucideIcon,
  Megaphone,
} from 'lucide-react';

export const Icons = {
  Zap,
  Sparkles,
  Droplets,
  FileText,
  Megaphone,
};

export type IconName = keyof typeof Icons;
